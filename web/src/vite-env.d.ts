/// <reference types="vite/client" />

// Declares the ambient module types for Vite's asset imports (`import logo from
// './x.png'` resolving to a URL string). Without this the build cannot type an
// image import at all — see NobleMark.tsx.
